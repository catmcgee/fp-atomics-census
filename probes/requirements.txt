# Exploratory GPU dependencies, not a historic-stack reconstruction lock.
# Install engines separately when their dependency constraints conflict.
# Legacy observed versions and missing provenance are in probes/stacks.json.
# For a new campaign, resolve explicit versions for the chosen GPU image,
# archive pip freeze, model revisions and binary hashes with the reports.
vllm
flashinfer-python
sglang[all]
